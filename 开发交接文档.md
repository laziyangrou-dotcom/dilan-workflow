# 帝蓝工作流 · 开发交接文档（截至 V45）

> 本文档供新的 Claude Code 会话（或新开发者）接手时快速获得完整上下文。
> 硬性规则的精简版在根目录 `CLAUDE.md`（Claude Code 会自动读取）；本文是完整背景。
> 语言约定：与用户全程中文交流；提交信息中文。

---

## 一、项目是什么

帝蓝工作流是一套 Windows 本地运行的 AI 影视内容生产工具，服务"美术素材 → 分镜生图 → 视频生成 → 审核"流水线。仓库含三个可独立启动的程序（文件夹名固定 `-V38`，**不随版本改名**）：

| 文件夹 | 作用 | 入口 |
|---|---|---|
| `帝蓝工作流-整合版-V38` | **制作端**（美术/分镜/视频三模块 + 网关），日常开发主战场 | `安装.bat` → `start.bat` |
| `帝蓝工作流-数据中心-V38` | 工程包上传/下载/合并仓库（sqlite + storage/） | `server.py` |
| `帝蓝工作流-统一审核端-V38` | 审核工作区 | `review_server.py` |

根目录 `升级搬家-V27数据迁移到V38.bat`：把旧 `-V27` 文件夹的运行数据（projects/input/output/usage.json 等）复制进 `-V38` 文件夹，不碰代码文件。

## 二、架构与数据模型

### 2.1 进程与端口
`integrated_server.py`（网关 **:8787**）启动并代理三个子服务：
- 美术 material **:8790**、分镜 image **:8788**、视频 video **:8789**（环境变量 `DILAN_*_PORT` 可覆盖）。
- 分流依据是 HTTP Referer / 路径前缀（`/material` 等）；统一入口 `http://127.0.0.1:8787/material`。
- 网关还负责：AK/SK 写入 Windows 用户环境变量的接口（`/api/system/set_tos_credentials`，用 `setx`）；大文件上传的**流式转发**（`proxy_stream`，http.client 按 1MB 块透传，不整包入内存）。

### 2.2 子服务
`tools/{material,image,video}/image_server.py`，各约 5600 行，`http.server.ThreadingHTTPServer`（**多线程并发处理请求**——这是全部并发问题的背景）。**三端高度同构**：同名处理器逻辑几乎一致，只有生成调用不同（美术/分镜走 GPT Responses / Gemini 生图，视频走火山方舟 Seedance 2.0）。改公共逻辑时用 Python 脚本对三份文件做锚点替换，并 `assert` 命中次数（本仓库历史提交都是这么做的）。

前端 = 各端单文件 `tools/*/index.html`，全部逻辑在**内联 `<script>`**。
⚠️ `tools/*/script.js` 是死文件，页面不加载它（V36 教训：把函数写进 script.js 导致"点击生成无反应"）。

### 2.3 project.json 结构（核心数据）
```
{ project_id, parent_id?, child_id?, updated_at, project_settings,
  assets:[{asset_id,name(全局唯一),category(人物/场景/道具/音频/视频/临时),file_path,group_id?,remote?{...TOS元数据,sha256},temporary?}],
  asset_groups:[{group_id,category,name,sort_order}],
  scenes:[{ scene_id, scene_code:"SC-0001", scene_number, expanded, review{...三轮审核},
    shots:[{ shot_id, shot_code, sort_order, active_tab_id, storyboard_candidates:[...],
      tabs:[{ tab_id, tab_name, settings, referenced_assets:[asset_id],
              messages:[{message_id,role,content,time,kind,image_id?,...}],
              generated_images:[{image_id,media_type?,file_path,thumb_path?,remote?,payload?,settings,cost_details?,generation_seconds?,...}],
              generated_videos: 与 generated_images 是**同一数组的镜像引用**,
              last_video_status:{state:running|success|error,...}（仅视频端使用）,
              current_base_image_id, draft_prompt, context_summary }]}]}]}
```
- 父子项目：父项目跨三端共享（`shared_project_index`），子项目引用格式 `parent{PROJECT_SEP}child`。
- **美术端特有**：`material_workspaces{character,scene,object}` 三个工作区槽，各存独立 scenes；顶层 `scenes` 是当前工作区的投影，`active_material_workspace` 是顶层 scenes 归属的**权威字段**（V34 修过"猜工作区导致 A 区内容覆盖 B 区"）。前端 `materialApplyServerDataPreserveWorkspace` 负责把服务器返回归位。
- 存储位置：`tools/<模块>/projects/<pid>/project.json`；素材文件 `input/<pid>/<分类>/`；生成结果 `output/image|video/<pid>/<SC>/<shot>/<tab>/`；临时参考 `projects/<pid>/temp_refs/`。生成文件**永不因 project.json 覆盖而删除**——磁盘文件只被专门的删除接口清理。

## 三、外部服务集成

### 3.1 火山 TOS（对象存储）——为什么存在
Ark（火山方舟）会直接断开几十 MB 的 base64 请求体（WinError 10054）。因此 V30–V35 起：**所有参考素材（图片/音频/视频）与生成视频转存都走 TOS**，请求里只放**预签名 URL**。
- 配置在设置页（`/api/config`：`tos_enabled / tos_bucket / tos_region / tos_endpoint / tos_upload_generated_videos`），已预填默认值。
- AK/SK **只**从环境变量读：`VOLC_TOS_ACCESS_KEY_ID` / `VOLC_TOS_SECRET_ACCESS_KEY`（兼容 `TOS_ACCESS_KEY_ID`/`TOS_SECRET_ACCESS_KEY`）。设置页可写入用户环境变量（重启生效）。**绝不写入 json/代码/localStorage——用户明确铁律。**
- `ensure_asset_remote_url`：按文件 sha256 去重上传，结果持久化到 `asset["remote"]`（在项目锁内完成，防并发互相覆盖）。
- 生成视频完成后自动转存 TOS（`build_tos_generated_video_key`），失败不影响本次生成（记 `remote_upload_error`）。
- API Key（Ark/OpenAI/Gemini）由前端 localStorage 全局设置保存、随请求体传给后端，**不落盘到 config.json**（后端 `data.pop("apiKey")`）。

### 3.2 生成通道
- 视频端：Seedance 2.0（`api_video_generate` → `submit_and_wait_seedance` 轮询）；`gen_count` 支持一次提交多条并发任务（前端 `launchVideoTask` 每条独立计时、独立合并）。
- 分镜端：GPT Responses 生图（`_perform_gpt_responses_*`）/ Gemini；美术端同构。
- 费用预估 `estimate_seedance_cost`，用量记录 `record_usage`（usage.json）。

## 四、并发防吞体系（V42–V44，最重要的设计）

**背景故障**："我开了很多子栏生视频，一个生成好了，点了下新增子栏，生成好的视频就丢了（根目录文件还在）"；随后又出现"视频闪现即消失、重新计时""任务永远显示生成中"；再后来"父栏随机开合"。本质都是**同一类竞态**：多线程/多请求对同一 project.json 或前端内存 project 做"整份 load→改→save/替换"，旧快照后到覆盖新状态。

### 4.1 后端（V42，三端 image_server.py）
- `project_mutation_lock(pid)`：每项目一把 RLock（模块级字典缓存）。
- `begin_project_mutation(self, pid)`：**取锁 + 锁内重读**最新 project 返回；锁挂在 `self._held_locks`，由 `do_POST` 的 `finally: _release_held_locks()` 统一释放（异常不泄漏）。**26 个结构/素材/存储/分镜候选处理器**（scene/shot 增删插改序、素材组、素材上传改名移动删除、storage 删除改名、storyboard、review 备注图、project_rename、temp_upload、image_delete）全部已改造为此模式。**以后新写任何 load+save 处理器都必须用它。**
- 生成路径：慢外呼（Seedance 轮询/AI 请求/TOS 上传首个）在锁外；"重读→追加结果→保存"在锁内。`api_video_generate` 里 payload 构建（会写 asset.remote）也在锁内。
- `/api/project/save`（前端整份快照）：锁内先 `merge_generated_media_preserve(client_data, on_disk)` —— 磁盘 tab 里的 generated_images/messages **按 id 只增不减**并回客户端快照（生成结果只被专门删除接口移除，所以并集安全、不会复活已删项）；`last_video_status` 若磁盘已是 success/error 而客户端还是 running/缺省，以磁盘终态为准。⚠️ 这个并集**只能放在 api_project_save**，放进底层 `save_project()` 会破坏删除接口。

### 4.2 前端（V43/V44，三端 index.html）
- `adoptProject(newData, removedIds)`（视频/分镜端）、`materialUnionGenMedia`（美术端，覆盖顶层 + 全部工作区槽）：
  1. 按 image_id 并集补回本地已知生成结果（防旧快照吞掉刚完成的）；
  2. `removedIds`（刚删除的 id）排除，防复活——删除调用处已传入；
  3. **视图状态以本会话内存为准**：`scene.expanded`、`shot.active_tab_id` 不被旧快照翻转（服务器新增的 SC/标签沿用服务器值；本地 active_tab 已不存在则回退）；
  4. 视频端：无在飞任务（pendingState 空）且本地已是 success/error 时，不被退回 running（消除"永远生成中"假状态）。
  **禁止在任何新代码里裸写 `project = r.data`。**
- `saveProject()` **单飞 + 合并**：`__saveInFlight`/`__saveQueued`，同一时刻最多 1 个保存在飞 + 1 个待发；待发合并所有后续调用，发送时序列化**最新** project。保证落盘顺序=操作顺序（父栏随机开合的另一半根因），也消除保存风暴。
- `mergeVideoResult`/`mergeGenResult`：生成完成的**定向合并**（只追加该条结果+消息+状态），不整份替换。
- 生成期间切换项目：`launchVideoTask` 记录 `reqProject`，完成时若用户已切走，只提示"已保存到原项目"，不动当前视图。

### 4.3 已知取舍（有意为之，向用户说明过）
- 并集"宁可多留、不可吞掉"：极小窗口内（删除与另一次整份保存几十毫秒内并发且删除先落盘）被删条目可能重现——文件本就在磁盘，重删一次即可。彻底方案（未做）：前端保存时携带"已知结果 id 清单"由服务端精确对齐。
- 多客户端同时开同一项目时，视图状态（折叠/当前标签）本就是共享字段，互相覆盖属预期；单机使用无影响。

## 五、性能约定（V44）

视频多（几十条）时曾"越来越卡直至卡死"，三个原因与对策（主要在视频端）：
1. `<video>` 一律 `preload="none"`（`videoTag()`）：重渲染重建全部视频元素时不再预载/解码；封面用服务器缩略图（生成时 `create_video_thumbnail`）或本地截帧缓存 `__posterCache`。
2. 无封面老视频：`hydrateVideoThumbnails` 每轮只补截 ≤8 条，截过进 `__posterCache` 复用；不再每次重渲染全量 seek+解码（`capturePoster` 会写缓存）。
3. `renderAll()` 只渲染 `currentScreen` 对应页面（工作台/素材库/存储库三选一）；`showScreen` 切页时补渲染。存储库（全部生成视频的网格）不再被每次操作重建。
另：保存串行化（见 4.2）本身大幅减少了大 JSON 的重复序列化/传输。若未来仍变慢，候选优化：聊天消息虚拟滚动/分页、瘦身 generated_images 里的历史 payload 字段。

## 六、UI/交互既定行为（用户验收过，别回退）

- **提交任务后提示词保留不清空**（草稿 `drafts[tabId]` + `draft_prompt` 持久化；重做 redoFromMessage 会填回历史提示词）。
- 重渲染**必须保持滚动位置**：模式是 `captureViewState()` → 重渲染 → `restoreViewState()`（含工作区滚动、每个聊天框 scrollTop、提示词框选区/高度）。`capturePromptStates` 同理。新增会重渲染的交互时务必沿用。
- 生成中显示实时计时（`pendingState[tabId]` 数组 + `.gen-elapsed` 每秒刷新 + `last_video_status.running` 刷新页面后仍显示）；完成显示"生成总时间 X 秒"（`generation_seconds`，服务端计）。
- 完成提示音（V44 增强）：三连上行琶音 880→1175→1568Hz 收尾重复，triangle 波，音量 0.5，约 1.1s，AudioContext suspended 时自动 resume；右下角绿色 toast。三端一致（`notifyNewAsset`）。
- @素材 引用：只允许当前标签页引用素材（`referenced_assets`）；改名素材会同步替换全项目提示词中的 @旧名（`replace_at_mentions_in_project`）。素材名全局唯一。
- 审核三轮（一审/二审/三审）：每轮锁定后不可改，锁定的 SC 前端禁编辑（`assertEditScene/assertEditShot`）。
- 删除生成视频有两级：聊天区删除（保留文件）与存储库彻底删除（删文件）。

## 七、大文件流式导入（V41）

工程包/素材包导入曾把整包 base64 读进内存导致大包内存爆炸。现行链路：
前端 `apiUploadBinary`（File 二进制直传，URL 参数放元信息，中文用 URLSearchParams 编码）→ 网关 `proxy_stream`（1MB 块透传）→ 子服务 `_stream_body_to_file`（落盘临时文件）→ `*_from_zip_path`（zipfile 从磁盘增量解压，`write_zip_member_to` 用 copyfileobj + **zip-slip 路径防护**）。
- 旧 base64 JSON 路径仍保留做兼容（`/api/project/import` vs `/api/project/import/stream`）。
- 实测：400MB 包子服务峰值 RSS 39MB（基线 37）；150MB 过网关峰值 23MB（基线 21）。
- V37 做过老版本工程包/素材包格式兼容。

## 八、历史坑清单（每条都真实踩过）

| 坑 | 结论 |
|---|---|
| `tools/*/script.js` 是死文件 | 只改 index.html 内联脚本（V36） |
| 本地预检 Ark 规则误拦（6.2s 音频被拦） | 预检已撤销，以 Ark 官方报错为准（V39）；报错有 `build_seedance_error_report` 中文解释 |
| base64 大请求体被 Ark 断开（WinError 10054） | 参考素材全走 TOS 预签名 URL（V35） |
| Windows 改名覆盖报 WinError 183 | 用 `safe_replace_file`/`os.replace`，且落库失败要回滚磁盘改名（V29/V40 期间） |
| 删除后同名重传素材显示旧图 | 浏览器缓存 + 同路径复用问题，V40 修复（文件名避让 `unique_name_with_paren`） |
| 美术工作区互相覆盖 | 顶层 scenes 归属以 `active_material_workspace` 为权威，不猜（V34） |
| 并发整份覆盖吞数据/翻状态 | 见第四章，新代码必须遵守不变量 |
| 重渲染弹回顶部 | captureViewState/restoreViewState 模式（V34 起） |
| 版本号改漏 | 全局 grep 上一版本号（如 "V44"）批量替换，验证余留只剩 TOS 文档的"V41 起" |

## 九、验证与测试方法论（沙箱内可复现）

1. **后端语法**：`python3 -m py_compile 三端 image_server.py`（+ 被改到的其他 .py）。
2. **前端语法**：正则 `<script(?![^>]*\bsrc=)[^>]*>(.*?)</script>`（DOTALL）提取内联脚本，逐块 `node --check`。
3. **前端行为**：用正则从 index.html 提取**真实函数源码**（如 adoptProject/saveProject/videoTag/materialUnionGenMedia）拼进 Node 测试文件，stub 掉 api/setSave/esc 等依赖，跑断言（本仓库 V43/V44 各留有 8–16 项断言的先例：并集、防复活、视图状态、保存单飞合并、封面缓存……）。
4. **后端并发/逻辑**：`importlib` 加载 `image_server.py`，把 `PROJECTS_DIR/INPUT_DIR/OUTPUT_*` 指到临时目录，`threading` 压测（V42 先例：对照组复现无锁丢视频，修复组 30 视频×30 结构操作零丢失）。
5. **沙箱边界**：可 import、可本地起服务自测；**连不上** TOS/Ark 等外网域名——涉及外呼的只能做逻辑级验证，真实生成由用户回归。
6. 批量改三端同构代码：写 Python 替换脚本，`assert` 每处锚点恰好命中 1 次（或 N 次），防止静默漏改。

## 十、版本大事记（产品版本 ≠ 文件夹名）

| 版本 | 内容 |
|---|---|
| V27 | 三程序基线导入仓库 |
| V28–V29 | 三端并发多任务生成+实时计时；导出按父栏-子栏命名；提交后保留提示词；WinError 183 修复；确立"每次修改版本号+1"铁律 |
| V30–V33 | Seedance 2.0 参考视频 TOS 云端化；安装脚本装 tos 依赖；AK/SK 写用户环境变量 + 设置页；TOS 全量配置进设置页 |
| V34 | 美术工作区互相覆盖修复（active_material_workspace 权威）；聊天框滚动位置保持 |
| V35 | 参考素材（图/音/视频）全走 TOS，修 10054 |
| V36 | "点击生成无反应"——captureViewState 补进内联脚本（发现 script.js 是死文件） |
| V37 | 兼容往期工程包/素材包导入 |
| V38 | 文件夹改名 -V38；加提交预检（后撤销） |
| V39 | 撤销预检拦截（用户要求） |
| V40 | 删除后同名重传素材显示旧图修复 |
| V41 | 大文件流式导入（前端二进制直传/网关流式代理/子服务落盘解压），实测内存达标 |
| V42 | **后端并发锁**：26 处理器 begin_project_mutation；api_project_save 生成结果并集（修"生成结果被吞"） |
| V43 | **前端 adoptProject**：整份采纳时并集+防复活+终态不退回（修"闪现即消失/永远转圈"）；后端状态兜底 |
| V44 | 保存串行化 + 视图状态保护（修"父栏随机开合"）；preload=none+封面缓存+分屏渲染（修视频多卡死）；提示音增强 |
| V45 | 本交接文档 + CLAUDE.md 入库 |

## 十一、当前状态与建议回归点

- 分支 `claude/stoic-fermat-g9avon` 已推送至 V45；工作树干净；三端 py_compile / node --check 全过。
- 无已知未修复 bug。用户上一轮反馈的三问题（父栏随机开合、视频多卡死、提示音弱）已在 V44 修复，**等待用户实机回归**：
  1. 快速连点多个 SC 折叠按钮——开合应精准对应所点；
  2. 大量视频工程的滑动/操作流畅度；生成完成留意新提示音；
  3. 并发生视频时新增子栏/切标签/保存——完成的视频稳定保留且状态"生成完成"。
- 如再报"变慢"：优先查聊天消息量与 project.json 体积（payload 历史），考虑消息虚拟滚动与 payload 瘦身。
