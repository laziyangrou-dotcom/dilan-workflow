# 锦童工作流 · 开发交接文档（截至 V1，原 V47 序列已重置）

> 本文档供新的 Claude Code 会话（或新开发者）接手时快速获得完整上下文。
> 硬性规则的精简版在根目录 `CLAUDE.md`（Claude Code 会自动读取）；本文是完整背景。
> 语言约定：与用户全程中文交流；提交信息中文。

---

## 一、项目是什么

锦童工作流（原名帝蓝工作流，V1 起品牌改为锦童）是一套 Windows 本地运行的 AI 影视内容生产工具。**现在仓库只有一个程序**：

| 文件夹 | 作用 | 入口 |
|---|---|---|
| `帝蓝工作流-整合版-V38`（物理目录名保留不改，护住本地数据） | 制作端（**美术 / 视频** 两模块 + 网关） | `安装.bat` → `start.bat` |

> **大精简（原 V47，用户明确要求）**：删除了「数据中心」「统一审核端」两个配套程序、整合版内的「分镜(image)」模块，以及整合版里所有数据中心上传下载与三轮审核/审核备注功能，只保留**工程包/素材包的本地导出与导入**。被删代码都在 git 历史里（V46 是删除前最后一版）。不要把这些已删子系统当作现存功能来改。

根目录 `升级搬家-V27数据迁移到V38.bat`：把旧 `-V27` 文件夹的运行数据复制进 `-V38` 文件夹（现只迁移 material/video 两模块）。

## 二、架构与数据模型

### 2.1 进程与端口
`integrated_server.py`（网关 **:8787**）启动并代理两个子服务：
- 美术 material **:8790**、视频 video **:8789**（环境变量 `DILAN_*_PORT` 可覆盖）。
- 分流依据是 HTTP Referer / 路径前缀（`/material`、`/video`）；统一入口 `http://127.0.0.1:8787/material`。Referer 无法识别时默认路由到 material。
- 网关还负责：AK/SK 写入 Windows 用户环境变量的接口（`/api/system/set_tos_credentials`，用 `setx`）；大文件上传的**流式转发**（`proxy_stream`，http.client 按 1MB 块透传，不整包入内存）。

### 2.2 子服务
`tools/{material,video}/image_server.py`，各约 5000+ 行，`http.server.ThreadingHTTPServer`（**多线程并发处理请求**——这是全部并发问题的背景）。**两端高度同构**：同名处理器逻辑几乎一致，只有生成调用不同（美术走 GPT Responses / Gemini 生图，视频走火山方舟 Seedance）。改公共逻辑时用 Python 脚本对两份文件做锚点替换，并 `assert` 命中次数（本仓库历史提交都是这么做的）。

前端 = 各端单文件 `tools/*/index.html`，全部逻辑在**内联 `<script>`**（现在每端只有 1 个脚本块）。
⚠️ `tools/*/script.js` 是死文件，页面不加载它（V36 教训）。

### 2.3 project.json 结构（核心数据）
```
{ project_id, parent_id?, child_id?, updated_at, project_settings,
  assets:[{asset_id,name(全局唯一),category(人物/场景/道具/音频/视频/临时),file_path,group_id?,remote?{...TOS元数据,sha256},temporary?}],
  asset_groups:[{group_id,category,name,sort_order}],
  scenes:[{ scene_id, scene_code:"SC-0001", scene_number, expanded,
    shots:[{ shot_id, shot_code, sort_order, active_tab_id, storyboard_candidates:[...],
      tabs:[{ tab_id, tab_name, settings, referenced_assets:[asset_id],
              messages:[...], generated_images:[...],
              generated_videos: 与 generated_images 是**同一数组的镜像引用**,
              last_video_status:{state:running|success|error,...}（仅视频端使用）,
              current_base_image_id, draft_prompt, context_summary }]}]}]}
```
- 父子项目：父项目跨两模块共享（`shared_project_index`，`PROJECT_MODULE_KEYS=["material","video"]`），子项目引用格式 `parent{PROJECT_SEP}child`。
- **美术端特有**：`material_workspaces{character,scene,object}` 三个工作区槽，各存独立 scenes；顶层 `scenes` 是当前工作区的投影，`active_material_workspace` 是顶层 scenes 归属的**权威字段**（V34 修过串槽）。前端 `materialApplyServerDataPreserveWorkspace` 负责归位。
- 存储位置：`tools/<模块>/projects/<pid>/project.json`；素材 `input/<pid>/<分类>/`；生成结果 `output/image|video/<pid>/<SC>/<shot>/<tab>/`；临时参考 `projects/<pid>/temp_refs/`。生成文件**永不因 project.json 覆盖而删除**——只被专门删除接口清理。
- **历史 review 字段**：老工程里可能残留 `scene.review`、`shot.review_notes_by_round`、`review_notes/` 目录——审核功能已删除，这些是**惰性数据**，加载/保存不处理也不报错；导入老工程包时 review_notes 文件被跳过。前端保留 `sceneLocked/canEdit*/assertEdit*` 的"永远可编辑" stub（全页大量调用点依赖这些名字，别删）。

## 三、外部服务集成

### 3.1 火山 TOS（对象存储）——为什么存在
Ark（火山方舟）会直接断开几十 MB 的 base64 请求体（WinError 10054）。因此 V30–V35 起：**所有参考素材（图片/音频/视频）与生成视频转存都走 TOS**，请求里只放**预签名 URL**。
- 配置在设置页（`tos_enabled / tos_bucket / tos_region / tos_endpoint / tos_upload_generated_videos`），已预填默认值。
- AK/SK **只**从环境变量读：`VOLC_TOS_ACCESS_KEY_ID` / `VOLC_TOS_SECRET_ACCESS_KEY`（兼容 `TOS_ACCESS_KEY_ID`/`TOS_SECRET_ACCESS_KEY`）。设置页可写入用户环境变量（重启生效）。**绝不写入 json/代码/localStorage——用户明确铁律。**
- `ensure_asset_remote_url`：按文件 sha256 去重上传，结果持久化到 `asset["remote"]`（在项目锁内完成）。
- 生成视频完成后自动转存 TOS，失败不影响本次生成（记 `remote_upload_error`）。
- API Key（Ark/OpenAI/Gemini）由前端 localStorage 全局设置保存、随请求体传给后端，**不落盘到 config.json**。

### 3.2 生成通道
- 视频端：Seedance（`api_video_generate` → `submit_and_wait_seedance` 轮询）。模型三选（显示名）：`锦童动视2.0` / `锦童动视2.0fast` / `锦童动视2.5`（ID doubao-seedance-2-0-260128 / -fast-260128 / 2-5-260628；旧名 seedance* 兼容归一）。**2.5 严格按官方示例只发 model/content/ratio/duration/generate_audio/watermark，不发 resolution/seed**；fast 的 1080p 强制降 720p 只作用于 fast。`gen_count` 支持一次提交多条并发任务。
- 美术端：锦童智能生图（GPT Responses，`_perform_gpt_responses_*`）/ 锦童图形编辑·锦童图形编辑fast（Gemini 3 Pro Image / 3.1 Flash Image；旧名 gpt生图 / Nano Banana Pro / Nano Banana 2 兼容归一）。
- 费用预估 `estimate_seedance_cost`：2.0 无/有视频 46/28、fast 37/22、2.5 无/有视频 70/42（¥/百万token，设置页可改）；用量记录 usage.json。

## 四、并发防吞体系（V42–V44，最重要的设计）

**背景故障**：并发生成 + 结构操作（新增子栏等）互相覆盖，出现"生成好的视频丢失/闪现即消失/永远生成中/父栏随机开合"。本质都是**整份 load→改→save/替换的旧快照覆盖竞态**。

### 4.1 后端（V42，两端 image_server.py）
- `project_mutation_lock(pid)`：每项目一把 RLock。
- `begin_project_mutation(self, pid)`：**取锁 + 锁内重读**；锁挂在 `self._held_locks`，由 `do_POST` 的 `finally: _release_held_locks()` 统一释放。全部结构/素材/存储/分镜候选处理器都已是此模式。**新写任何 load+save 处理器必须用它。**
- 生成路径：慢外呼（Seedance 轮询/AI 请求）在锁外；"重读→追加结果→保存"在锁内。
- `/api/project/save`：锁内先 `merge_generated_media_preserve(client_data, on_disk)`——磁盘生成结果/消息**按 id 只增不减**并回；`last_video_status` 磁盘终态(success/error)不被退回 running。⚠️ 并集**只能放在 api_project_save**，放进底层 `save_project()` 会破坏删除接口。

### 4.2 前端（V43/V44，两端 index.html）
- `adoptProject(newData, removedIds)`（视频端）、`materialUnionGenMedia`（美术端，覆盖顶层+全部工作区槽）：并集生成结果、removedIds 防复活、**视图状态（expanded/active_tab_id）以本会话内存为准**、视频端无在飞任务时终态不退回 running。**禁止裸写 `project = r.data`。**
- `saveProject()` **单飞 + 合并**：最多 1 个在飞 + 1 个待发，发送时取最新 project。
- `mergeVideoResult`/`mergeGenResult`：生成完成的定向合并，不整份替换。
- 已知取舍：并集"宁可多留、不可吞掉"——极小窗口下删除+整份保存并发可能让被删条目重现（文件本就在磁盘，重删即可）。

## 五、性能约定（V44）

1. 生成视频 `<video>` 一律 `preload="none"`；封面走服务器缩略图或 `__posterCache`。
2. 无封面老视频每轮最多补截 8 条，截过进缓存复用。
3. `renderAll()` 只渲染 `currentScreen` 对应页面；`showScreen` 切页补渲染。
若再变慢：查聊天消息量与 project.json 体积（payload 历史），考虑消息虚拟滚动与 payload 瘦身。

## 六、UI/交互既定行为（用户验收过，别回退）

- **提交任务后提示词保留不清空**（`drafts[tabId]` + `draft_prompt` 持久化）。
- 重渲染**必须保持滚动位置**：`captureViewState()` → 重渲染 → `restoreViewState()`（含聊天框 scrollTop、提示词框选区/高度）。
- 生成中实时计时（`pendingState`/`genTasks` + `.gen-elapsed` 每秒刷新 + 视频端 `last_video_status.running` 刷新页面后仍显示）；完成显示"生成总时间 X 秒"。
- 完成提示音（V44 增强）：三连上行琶音 880→1175→1568Hz 收尾重复，音量 0.5、约 1.1s，AudioContext suspended 自动 resume；右下角绿色 toast。
- @素材 引用：只允许当前标签页引用素材；改名素材同步替换全项目提示词中的 @旧名。素材名全局唯一。
- 删除生成结果两级：聊天区删除（保留文件）与存储库彻底删除（删文件）。
- 待选分镜/分镜视频面板（storyboard_candidates）是**视频/美术端自身功能**（与已删除的"分镜模块"无关），保留。

## 七、大文件流式导入（V41）

前端 `apiUploadBinary`（File 二进制直传）→ 网关 `proxy_stream`（1MB 块透传）→ 子服务 `_stream_body_to_file` → `*_from_zip_path`（zipfile 磁盘增量解压，`write_zip_member_to` 带 **zip-slip 防护**）。旧 base64 JSON 路径保留兼容。实测：400MB 包子服务峰值 RSS 39MB；150MB 过网关峰值 23MB。V37 兼容老包格式；老分镜工程包导入会被明确拒绝（"分镜模块已从本工作流移除"）。

## 八、历史坑清单（每条都真实踩过）

| 坑 | 结论 |
|---|---|
| `tools/*/script.js` 是死文件 | 只改 index.html 内联脚本（V36） |
| 本地预检 Ark 规则误拦 | 预检已撤销，以 Ark 官方报错为准（V39） |
| base64 大请求体被 Ark 断开（10054） | 参考素材全走 TOS 预签名 URL（V35） |
| Windows 改名覆盖报 WinError 183 | `safe_replace_file`/`os.replace`，落库失败回滚磁盘改名 |
| 删除后同名重传素材显示旧图 | V40 修复（文件名避让） |
| 美术工作区互相覆盖 | 顶层 scenes 归属以 `active_material_workspace` 为权威（V34） |
| 并发整份覆盖吞数据/翻状态 | 第四章不变量 |
| 重渲染弹回顶部 | captureViewState/restoreViewState 模式 |
| `float(a if c else b or 0)` 括号陷阱 | `or 0` 只作用 else 分支，配置键缺失时 float(None) 崩溃（V46 修） |
| 版本号改漏 | 全局 grep 上一版本号批量替换，验证余留只剩 TOS 文档"V41 起" |

## 九、验证与测试方法论（沙箱内可复现）

1. **后端语法**：`python3 -m py_compile` 两端 image_server.py + integrated_server.py。
2. **前端语法**：正则 `<script(?![^>]*\bsrc=)[^>]*>(.*?)</script>`（DOTALL）提取内联脚本，逐块 `node --check`。
3. **前端行为**：正则提取**真实函数源码**拼进 Node 测试文件，stub 依赖后跑断言（V43/V44/V46 均有先例）。
4. **后端并发/逻辑**：`importlib` 加载 `image_server.py`，把 `PROJECTS_DIR/INPUT_DIR/OUTPUT_*` 指到临时目录，threading 压测。
5. **沙箱边界**：可 import、可本地起服务自测；**连不上** TOS/Ark 外网域名——真实生成由用户回归。
6. 批量改两端同构代码：Python 替换脚本 + `assert` 锚点命中次数。

## 十、版本大事记（产品版本 ≠ 文件夹名）

| 版本 | 内容 |
|---|---|
| V27–V29 | 基线；并发多任务生成+计时；提交后保留提示词；确立"每次修改版本号+1"铁律 |
| V30–V35 | TOS 云端化（配置页/AK-SK 环境变量/全量参考走 TOS 修 10054） |
| V36–V40 | script.js 死文件教训；老包兼容；预检加了又撤；同名素材缓存修复 |
| V41 | 大文件流式导入（三层流式链路），实测内存达标 |
| V42 | 后端并发锁（begin_project_mutation + api_project_save 并集） |
| V43 | 前端 adoptProject（并集+防复活+终态不退回）；后端状态兜底 |
| V44 | 保存串行化 + 视图状态保护（修父栏随机开合）；preload=none+封面缓存+分屏渲染（修视频多卡死）；提示音增强 |
| V45 | 交接文档 + CLAUDE.md 入库 |
| V46 | 视频模块新增 seedance2.5（官方示例对齐：不发 resolution/seed；单价 70/42）；修 float 括号隐患 |
| 原V47 | **大精简**：删除数据中心/统一审核端两程序、分镜模块、整合版内数据中心与审核（三轮审核/锁定/审核备注）全部功能；保留工程包/素材包本地导出导入；父项目共享与网关只覆盖 material/video |

## 十一、当前状态与建议回归点

- 分支 `claude/stoic-fermat-g9avon`；版本 **V1**（品牌重塑后重置的新序列，下次改动 V2）；两端 py_compile / node --check 全过，本地起服务冒烟通过。
- **等待用户实机回归**：
  1. 精简后完整走一遍：建父项目（应只生成美术/视频两模块）→ 两模块建子项目 → 生成 → 导出工程包/素材包 → 本地导入；
  2. seedance2.5 真机生成一条视频（沙箱连不上 Ark）；
  3. 确认界面上已无"上传项目/下载项目/数据中心地址/审核轮次/审核备注"入口。
- 老工程注意：含审核数据的老工程可正常打开（review 字段惰性保留）；老分镜工程包导入会被明确拒绝。

> **V1（品牌重塑）**：帝蓝→锦童全部显示字样；UI 换为浅粉暖色主题（参考锦童科技风格：页面底 #f8efe9、卡片白、强调玫瑰粉 #d98a94，状态点/视频黑底等语义色保留）；模型显示名改为 锦童动视2.0/2.0fast/2.5、锦童智能生图、锦童图形编辑、锦童图形编辑fast（API model id 不变，旧存值前后端双向兼容归一）；版本号序列自 V1 重新计数（10 处 6 文件）。物理文件夹名、localStorage 键、dilan_project_package 包类型、DILAN_* 环境变量等内部标识保留以兼容既有数据。
